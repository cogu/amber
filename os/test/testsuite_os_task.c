//////////////////////////////////////////////////////////////////////////////
// INCLUDES
//////////////////////////////////////////////////////////////////////////////
#include <assert.h>
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include "CuTest.h"
#include "os_task.h"
#ifdef MEM_LEAK_CHECK
#include "CMemLeak.h"
#endif


//////////////////////////////////////////////////////////////////////////////
// CONSTANTS AND DATA TYPES
//////////////////////////////////////////////////////////////////////////////

//////////////////////////////////////////////////////////////////////////////
// LOCAL FUNCTION PROTOTYPES
//////////////////////////////////////////////////////////////////////////////
static void test_os_task_create(CuTest* tc);
static void test_os_task_events(CuTest* tc);
THREAD_PROTO(test_task, arg);

//////////////////////////////////////////////////////////////////////////////
// GLOBAL VARIABLES
//////////////////////////////////////////////////////////////////////////////

//////////////////////////////////////////////////////////////////////////////
// LOCAL VARIABLES
//////////////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////////////
// GLOBAL FUNCTIONS
//////////////////////////////////////////////////////////////////////////////


CuSuite* testsuite_os_task(void)
{
	CuSuite* suite = CuSuiteNew();

	SUITE_ADD_TEST(suite, test_os_task_create);
	SUITE_ADD_TEST(suite, test_os_task_events);

	return suite;
}

//////////////////////////////////////////////////////////////////////////////
// LOCAL FUNCTIONS
//////////////////////////////////////////////////////////////////////////////

static void test_os_task_create(CuTest* tc)
{
	os_task_t task1;
   CuAssertIntEquals(tc, 0, os_task_create(&task1, test_task));
	os_task_destroy(&task1);
}

static void test_os_task_events(CuTest* tc)
{
   os_task_t task1;
   uint32_t eventMask;
   CuAssertIntEquals(tc, 0, os_task_create(&task1, test_task));
   CuAssertUIntEquals(tc, 0, task1.eventMask1);
   os_task_setEvent(&task1, 0x10);
   CuAssertUIntEquals(tc, 0x10, task1.eventMask1);
   CuAssertIntEquals(tc, 0, os_task_waitEvent(&task1, &eventMask));
   CuAssertUIntEquals(tc, 0x10, eventMask);
   os_task_destroy(&task1);
}


THREAD_PROTO(test_task, arg)
{
	THREAD_RETURN(0);
}
